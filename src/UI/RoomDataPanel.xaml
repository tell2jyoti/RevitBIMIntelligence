<Window x:Class="RevitBIMIntelligence.UI.RoomDataPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Data Extractor"
        Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#FAFAFA"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8F5E9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Room Data Summary"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="#1976D2"/>
            <TextBlock x:Name="SummaryText"
                       FontSize="14"
                       Foreground="#666"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1"
                  x:Name="RoomDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#E0E0E0"
                  BorderBrush="#E0E0E0"
                  BorderThickness="1"
                  RowHeaderWidth="0"
                  AlternationCount="2"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Room Number"
                                    Binding="{Binding RoomNumber}"
                                    Width="100"/>
                <DataGridTextColumn Header="Room Name"
                                    Binding="{Binding RoomName}"
                                    Width="*"/>
                <DataGridTextColumn Header="Level"
                                    Binding="{Binding Level}"
                                    Width="120"/>
                <DataGridTextColumn Header="Area (sqm)"
                                    Binding="{Binding AreaSquareMeters, StringFormat=N2}"
                                    Width="100"/>
                <DataGridTextColumn Header="Doors"
                                    Binding="{Binding DoorCount}"
                                    Width="70"/>
                <DataGridTextColumn Header="Windows"
                                    Binding="{Binding WindowCount}"
                                    Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="ExportButton"
                    Content="Export JSON"
                    Click="ExportButton_Click"
                    Padding="16,8"
                    Margin="0,0,8,0"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
            <Button x:Name="CloseButton"
                    Content="Close"
                    Click="CloseButton_Click"
                    Padding="16,8"
                    Background="#757575"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
